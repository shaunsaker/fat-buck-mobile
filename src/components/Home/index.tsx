import React from 'react';
import { HeaderBar } from '../HeaderBar';
import { Background } from '../Background';

export const Home = () => {
  return (
    <Background>
      <HeaderBar />
    </Background>
  );
};
